import Alert from '@/components/ui/Alert';

const Icon = () => {
  return (
    <div>
      <Alert showIcon>
        Additional description and information about copywriting.
      </Alert>
    </div>
  );
};

export default Icon;

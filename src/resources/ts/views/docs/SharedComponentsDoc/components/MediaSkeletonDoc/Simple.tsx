import MediaSkeleton from '@/components/shared/loaders/MediaSkeleton';

const Example = () => {
  return (
    <MediaSkeleton
      avatarProps={{
        width: 45,
        height: 45,
      }}
    />
  );
};

export default Example;
